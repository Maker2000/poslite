import 'package:flutter/material.dart';

class TotalAndCheckOut extends StatelessWidget {
  const TotalAndCheckOut({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [Text('total')],
    );
  }
}
